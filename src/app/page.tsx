import Hero from "@/components/Hero";

export default function HomePage() {
  // This page will now primarily display the Hero section as the landing page.
  // Other sections are now separate pages.
  return (
    <Hero />
  );
}
